@urs.map do | urs|
  {
    id: urs.id,
    user_id: urs.user_id,
    route_id: urs.route_id,
    shoe_id: urs.shoe_id,
    route_name: urs.route.name,
    route_city: urs.route.city,
    route_distance: urs.route.distance,
    shoe_description: urs.shoe.description,
    shoe_mileage: urs.shoe.mileage
  }
end