@users.map do |user|
  {
   id: user.id, 
    first_name: user.first_name,
    last_name: user.last_name,
    username: user.username,
    email: user.email,
    shoes: user.shoes,
    routes: user.routes
  }
end